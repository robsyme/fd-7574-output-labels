// Use this only for local testing, e.g.:
//   NXF_VER=26.04.3 nextflow run main.nf -c local.config -output-dir results
// On Platform, lineage comes from the workspace setting instead.
lineage {
    enabled = true
    store.location = './.lineage'
}
